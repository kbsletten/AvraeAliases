!attack add "Unarmed Strike (Acid)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}acid]" -desc "When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Unarmed Strike (Cold)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}cold]" -desc "When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Unarmed Strike (Fire)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}fire]" -desc "When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Unarmed Strike (Lightning)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}lightning]" -desc "When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Unarmed Strike (Poison)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}poison]" -desc "When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
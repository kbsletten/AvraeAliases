!attack add "Draconic Disciple (Acid)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}acid]" -f "Draconic Disciple|When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Draconic Disciple (Cold)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}cold]" -f "Draconic Disciple|When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Draconic Disciple (Fire)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}fire]" -f "Draconic Disciple|When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Draconic Disciple (Lightning)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}lightning]" -f "Draconic Disciple|When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."
!attack add "Draconic Disciple (Poison)" -b "{{max(dexterityMod, strengthMod) + proficiencyBonus}}" -d "1d{{'4' if MonkLevel < 5 else '6' if MonkLevel < 11 else '8' if MonkLevel < 17 else '10'}}+{{max(dexterityMod, strengthMod)}}[{{'magical ' if MonkLevel >= 6 else ''}}poison]" -f "Draconic Disciple|When you damage a target with an unarmed strike, you can change the damage type to acid, cold, fire, lightning, or poison."